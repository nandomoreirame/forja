# TASKS.md

- [ ] feature: --resume [sessionId]
  > Capturing session IDs from CLIs (Claude, Codex, Gemini) and restoring conversations with `--resume` is a good idea; parsing the CLI output to extract session IDs and caching them is also beneficial.
